// ? :    --- Operação ternária ---------- (condicao) ? 'Valor para verdadeiro' : 'Valor para falso';
const pontuacaoUsuario = 1000;
const nivelUsuario = pontuacaoUsuario >= 1000 ? 'Usuário Vip' : 'Usuário plebeu'; 

console.log(nivelUsuario);

/* if (pontuacaoUsuario >= 1000) {
    console.log('Vip, xuxuco');
} else {
    console.log('Plebeu');
} */
